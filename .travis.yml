language: php
php:
  - 7.1.9
script:
  - composer install
  - cp .env.example .env
  - php artisan key:generate
  - php artisan migrate
  - vendor/bin/phpunit
deploy:
  provider: heroku
  api-key: 
    secure: $HEROKU_API_KEY
  app: projetoesphp
  strategy: git
  run: "php artisan migrate"
